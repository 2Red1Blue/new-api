# Requirements

- Fetch the latest `upstream/main` without changing global proxy configuration.
- Merge it into local `main` and preserve all pre-existing tracked and untracked work in a recoverable stash.
- Resolve conflicts by retaining compatible fork behavior and upstream fixes.
- Validate the merged tree and restored working tree.
